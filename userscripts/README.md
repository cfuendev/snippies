### copy-picrew-metadata.js

Adds a button that copies the JSON metadata of the Picrew to your clipboard.

### svelte-repl-dark.js (archive)

Adds a Dark Mode toggle button to Svelte's official online REPL.

**NOTE:**
This u.s wasn't properly finished. (Still has some places where it needs improvement, such as the 'generated code' sections, where it just looks ugly)

### chatgpt-sidebar-toggle (deprecated, archive)

Adds a little button that peeks out of ChatGPT's taskbar which toggles the task bar.

**NOTE:**
- Was made redundant as soon as OpenAI put the site through it's first UI overhaul, which did include a sidebar toggle button.
- Probably doesn't work with all of the UI changes that ChatGPT has done since then (This was back when ChatGPT only had been out for less than a year)
